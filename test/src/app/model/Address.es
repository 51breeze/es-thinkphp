package app.model;

import server.model.Model;
import server.facade.Db;
import table.Address as AddressStruct;
import server.facade.Cache;
import server.database.Raw;

class Address extends Model<Address> implements AddressStruct {

}